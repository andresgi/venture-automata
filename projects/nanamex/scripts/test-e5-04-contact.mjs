import { readFileSync } from "node:fs";
import { spawn, spawnSync } from "node:child_process";
const sql = readFileSync(new URL("./test-e5-04-contact.sql", import.meta.url));
const probe = spawnSync("psql", ["-h", "127.0.0.1", "-p", "55322", "-U", "postgres", "-d", "postgres", "-c", "select 1"], { env: { ...process.env, PGPASSWORD: "postgres" } });
const base = probe.status === 0 ? ["psql", ["-h", "127.0.0.1", "-p", "55322", "-U", "postgres", "-d", "postgres", "-v", "ON_ERROR_STOP=1"]] : ["docker", ["exec", "-i", "supabase_db_nanamex", "psql", "-U", "postgres", "-d", "postgres", "-v", "ON_ERROR_STOP=1"]];
const run = (input) => new Promise((resolve, reject) => { const p = spawn(base[0], base[1], { env: { ...process.env, PGPASSWORD: "postgres" }, stdio: ["pipe", "inherit", "inherit"] }); p.on("close", (code) => code === 0 ? resolve() : reject(new Error(`psql exited ${code}`))); p.stdin.end(input); });
try {
  await run(sql);
  // Two independent sessions exercise the row lock/estado guard rather than only
  // sequential retries. Both callers must succeed and leave one contact/event;
  // one takes the Nueva branch and the other observes the committed idempotent result.
  await run("update public.entitlements set expires_at=now()+interval '30 days' where id='00000000-0000-0000-0000-000000000631'; update public.pipeline set estado='nueva' where id='00000000-0000-0000-0000-000000000612';");
  await Promise.all([
    run("select public.confirm_contact('00000000-0000-0000-0000-000000000602','00000000-0000-0000-0000-000000000621','00000000-0000-0000-0000-000000000623','Concurrent A');"),
    run("select public.confirm_contact('00000000-0000-0000-0000-000000000602','00000000-0000-0000-0000-000000000621','00000000-0000-0000-0000-000000000623','Concurrent B');"),
  ]);
  await run("do $$ declare c int; e int; s text; m text; begin select count(*), max(estado) into c,s from public.pipeline where id='00000000-0000-0000-0000-000000000612'; select count(*) into e from public.analytics_events where event_name='candidate_contacted' and necesidad_id='00000000-0000-0000-0000-000000000602'; select max(mensaje) into m from public.contacto where pipeline_id='00000000-0000-0000-0000-000000000612'; if c<>1 or s<>'contactada' or e<>1 or m not in ('Concurrent A','Concurrent B') then raise exception 'concurrent retry assertion failed'; end if; end $$;");
} finally { await run("delete from public.analytics_events where necesidad_id in ('00000000-0000-0000-0000-000000000601','00000000-0000-0000-0000-000000000602'); delete from public.contacto where pipeline_id in ('00000000-0000-0000-0000-000000000611','00000000-0000-0000-0000-000000000612'); delete from public.pipeline where necesidad_id in ('00000000-0000-0000-0000-000000000601','00000000-0000-0000-0000-000000000602'); delete from public.entitlements where familia_id in ('00000000-0000-0000-0000-000000000621','00000000-0000-0000-0000-000000000622'); delete from public.necesidad_children where necesidad_id in ('00000000-0000-0000-0000-000000000601','00000000-0000-0000-0000-000000000602'); delete from public.necesidades where id in ('00000000-0000-0000-0000-000000000601','00000000-0000-0000-0000-000000000602'); delete from public.perfil_familiar where profile_id in ('00000000-0000-0000-0000-000000000621','00000000-0000-0000-0000-000000000622'); delete from public.ninera_zonas where ninera_id='00000000-0000-0000-0000-000000000623'; delete from public.perfil_ninera where profile_id='00000000-0000-0000-0000-000000000623'; delete from public.profiles where id in ('00000000-0000-0000-0000-000000000621','00000000-0000-0000-0000-000000000622','00000000-0000-0000-0000-000000000623'); delete from auth.users where id in ('00000000-0000-0000-0000-000000000621','00000000-0000-0000-0000-000000000622','00000000-0000-0000-0000-000000000623'); delete from public.zonas where id='00000000-0000-0000-0000-000000000641';"); }
